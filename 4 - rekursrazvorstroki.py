def reverse_string(s):
    # Базовый случай: пустая строка или один символ
    if len(s) <= 1:
        return s
    # Берём последний символ + рекурсивно разворачиваем всё, кроме него
    return s[-1] + reverse_string(s[:-1])
